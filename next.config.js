// next.config.js (example if it was a simple config)
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add other Next.js configurations here
};

module.exports = nextConfig;